# Telegram Bot Framework
python-telegram-bot

# LLM APIs
google-generativeai
mistralai

# Vector Database & Embeddings
chromadb              # Клиент ChromaDB. БД должна быть внешней для Vercel!
sentence-transformers # Для эмбеддингов. Требует torch или tensorflow.
transformers          # Для токенизатора и sentence-transformers.
torch                 # Бэкенд для sentence-transformers/transformers.
sentencepiece         # Часто требуется для токенизаторов.

# Audio Processing
# ВНИМАНИЕ: pydub НЕ БУДЕТ работать на Vercel без установки ffmpeg!
pydub
SpeechRecognition     # Для транскрипции (использует внешние API).

# Image Processing
Pillow

# Environment Variables Helper (полезно для локальной разработки)
python-dotenv
